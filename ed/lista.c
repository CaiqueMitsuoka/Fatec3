#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

int main(){
    //Lista é um sequencia de coisas, unidimencional que obedece algumas regras:
    //L[1]é a primeira posição
    //L[n] é um elemento da Lista
    //L[n-1] é o elemento anterior a n
    //L[n+1] é o elemento seguinte a n
    //você pode operar a Lista
    //inserir
    //remover
    //acessar
    //determinar se x pertence
    //determinar total
    //ordenar Lista
    //lista com vetor é o vetor de matriz
    //lista encadeada é a lista de dadoscom referencia ao dado que vem antes e depois

    printf("%d\n%d\n",sizeof(int),sizeof(int*));
    return 0;
}
